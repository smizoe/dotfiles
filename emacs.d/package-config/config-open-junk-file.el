;;; config-open-junk-file.el ---                     -*- lexical-binding: t; -*-
;;; Code:

(use-package open-junk-file
 :ensure t
 :config
 (setq open-junk-file-format "~/junk/%Y-%m/%Y-%m-%d_%H-%M-%S_")
 )


(provide 'config-open-junk-file)
;;; config-open-junk-file.el ends here
