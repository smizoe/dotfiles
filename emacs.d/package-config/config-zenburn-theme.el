;;; config-zenburn-theme.el ---                      -*- lexical-binding: t; -*-

;;; Code:
(use-package zenburn-theme
  :ensure t
  :init
  (progn
    (load-theme 'zenburn t)
    )
  )


(provide 'config-zenburn-theme)
;;; config-zenburn-theme.el ends here
